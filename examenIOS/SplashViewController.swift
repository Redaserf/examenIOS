//
//  SplashViewController.swift
//  examenIOS
//
//  Created by Hugo Alexis Ramirez Villarreal on 12/05/25.
//

import UIKit

class SplashViewController: UIViewController {
//    private let imvAppLogo = UIImageView(image: UIImage(named: "logoDevelop"))

    override func viewDidLoad() {
        super.viewDidLoad()
        
        print("Hola desde splash")
        
//        UIView.animate(withDuration: 2) {
//            self.imvAppLogo.frame = CGRect(x: 0, y: 0, width: self.view.frame.width, height: self.view.frame.height)
//            
//            self.imvAppLogo.center = self.view.center
//        }

        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
